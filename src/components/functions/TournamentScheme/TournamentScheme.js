import * as React from 'react';
import {Button} from '@mui/material';

// const gridStyle = {
//     minHeight: "85vh"
// };

export function TournamentScheme() {
    return(
        <Button fullWidth type="submit" size="large" variant="contained" color="secondary">FINISH!</Button>
    )
}